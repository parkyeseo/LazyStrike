name: lazy-strike
channels: [pytorch, nvidia, conda-forge, defaults]
dependencies:
  - python=3.10
  - pytorch=2.1.*
  - torchvision=0.16.*
  - pytorch-cuda=11.8
  - numpy=1.26.*
  - pillow
  - scipy=1.11.*
  - matplotlib=3.8.*
  - pip
  - pip:
      - timm==0.9.16
      - wandb==0.16.*
      - einops==0.7.*
      - omegaconf==2.3.*
      - hydra-core==1.3.*
      - opencv-python-headless==4.9.*
      - tqdm
      - rich
      - pytest==8.*

